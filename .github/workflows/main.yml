name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses:jeffrey4l/jeffrey4l.github.io@source
    - name: Run a one-line script
      run: echo start build
    - name: Run a multi-line script
      run: |
        make github
